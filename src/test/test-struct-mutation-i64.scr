struct F {
        x: i64,
        y: i64,
}

export proc _start(void): ! {
        let f: F = F {
                x = 30,
                y = 38,
        };

        f.x += f.y + 1;

        exit f.x;
}
